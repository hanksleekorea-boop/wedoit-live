import {PRACTICE_PROMPTS,practiceKind,validateExecution} from './happyscan-practice-guide.mjs';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function practiceFields(action,note='',state={}){const id=action.id,kind=practiceKind(id),label={write:'짧게 적으며 실천하기',timed:'시간을 써서 관찰하기',contact:'안전한 연결을 선택하기',prepare:'작은 행동을 준비·실행하기'}[kind];return `<p class="badge neutral">${label}</p><label for="note-${id}">${esc(PRACTICE_PROMPTS[id])} <span class="small">선택</span></label><textarea id="note-${id}" data-action-note="${id}" maxlength="2000" rows="3">${esc(note)}</textarea><div class="field"><label for="minutes-${id}">직접 적는 대략의 실천 시간(분) · 선택</label><input id="minutes-${id}" data-action-execution="${id}" data-execution-field="minutes" type="number" min="0" max="1440" step="1" value="${esc(state.minutes??'')}"><label for="fit-${id}">이번 방법이 나에게 맞았나요? · 선택</label><select class="btn" id="fit-${id}" data-action-execution="${id}" data-execution-field="fit">${[['not-rated','평가하지 않음'],['comfortable','부담 없이 맞았음'],['adjust','조정해서 해보고 싶음'],['not-for-me','지금은 맞지 않음']].map(([v,t])=>`<option value="${v}" ${state.fit===v?'selected':''}>${t}</option>`).join('')}</select></div><p class="small muted">시간·적합성은 자기보고이며 행복 점수에 더하지 않습니다. 불편하거나 안전하지 않다면 멈추거나 쉬어도 괜찮아요. 다른 사람에게 메시지를 자동 발송하지 않습니다.</p>`;}
export function executionFromInput(id,minutes,fit){const raw=String(minutes??'').trim();return validateExecution({kind:practiceKind(id),minutes:raw===''?null:Number(raw),fit:fit||'not-rated'},id);}
