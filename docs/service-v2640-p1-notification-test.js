(()=>{
  "use strict";
  const copy=()=>document.documentElement.lang==="en"?{kicker:"Check notifications",title:"Send one test notification",intro:"Only when you choose this button, the browser can ask for notification permission and send one test. It never creates a repeating reminder.",button:"Send one test notification",unsupported:"This browser cannot send a test notification.",default:"Notification permission has not been chosen yet.",denied:"Notifications are blocked in this browser. Change the browser setting if you want to test again.",ready:"Permission is ready. Choose the test button to send one notification.",sent:"One test notification was sent. Check your device notification area.",failed:"The browser could not send the test notification.",note:"No schedule, account connection, or external data transfer is added."}:{kicker:"알림 확인",title:"시험 알림 한 번 보내기",intro:"이 버튼을 누를 때만 브라우저가 알림 권한을 묻고 시험 알림 한 번을 보낼 수 있어요. 반복 알림은 만들지 않습니다.",button:"시험 알림 한 번 보내기",unsupported:"이 브라우저에서는 시험 알림을 보낼 수 없어요.",default:"알림 권한을 아직 고르지 않았어요.",denied:"이 브라우저에서 알림이 차단되어 있어요. 다시 시험하려면 브라우저 설정을 바꿔 주세요.",ready:"권한을 사용할 수 있어요. 시험 버튼을 눌러 알림 한 번을 보내 보세요.",sent:"시험 알림 한 번을 보냈어요. 기기의 알림 영역을 확인해 보세요.",failed:"브라우저가 시험 알림을 보내지 못했어요.",note:"일정·계정 연결·외부 데이터 전송은 추가하지 않습니다."};
  const permission=()=>typeof Notification==="undefined"?"unsupported":Notification.permission;
  async function deliver(){const options={body:document.documentElement.lang==="en"?"This is your one test notification.":"시험 알림 한 번입니다.",tag:"wedoit-one-test",renotify:true};try{new Notification("HappyScan",options);return true}catch(_){const registration=await navigator.serviceWorker?.getRegistration?.();if(!registration?.showNotification)throw _;await registration.showNotification("HappyScan",options);return true}}
  function mount(){if(document.querySelector("#serviceNotificationTestSection"))return true;const anchor=document.querySelector("#serviceRecordSearchSection")||document.querySelector("#serviceNoteMoodSection")||document.querySelector("#pcNotificationSection"),host=document.querySelector("main.app");if(!anchor||!host)return false;const root=document.createElement("section");root.id="serviceNotificationTestSection";root.className="section service-notification-test";root.dataset.serviceView="me";anchor.insertAdjacentElement("afterend",root);let status="";const render=()=>{const t=copy(),state=permission(),message=status||(state==="unsupported"?t.unsupported:state==="denied"?t.denied:state==="granted"?t.ready:t.default);root.innerHTML=`<header><span class="service-section-kicker">${t.kicker}</span><h2>${t.title}</h2><p>${t.intro}</p></header><div class="service-notification-test-actions"><button class="primary" id="serviceNotificationTest" type="button" ${state==="unsupported"?"disabled":""}>${t.button}</button></div><p id="serviceNotificationTestStatus" class="service-notification-test-status" role="status">${message}</p><p class="service-notification-test-note">${t.note}</p>`;root.querySelector("#serviceNotificationTest")?.addEventListener("click",async()=>{if(permission()==="unsupported"){status=t.unsupported;render();return}try{const result=permission()==="granted"?"granted":await Notification.requestPermission();if(result!=="granted"){status=result==="denied"?t.denied:t.default;render();return}await deliver();status=t.sent}catch(_){status=t.failed}render()})};window.addEventListener("wedoit:languagechange",render);render();document.documentElement.dataset.p1NotificationTestReady="true";return true}
  let tries=0;const timer=setInterval(()=>{tries+=1;if(mount()||tries>250)clearInterval(timer)},40);window.__WEDOIT_V264_P1_NOTIFICATION_TEST__={permission};
})();
